# 外边距和内边距练习

对于第一个练习，只需编辑 `style.css` 文件，使 div 看起来像下面的图像。只在文件中指示的地方编辑 CSS。对于这个练习，你只需要更改外边距（margin）和内边距（padding）的值。你不需要在 CSS 中添加或删除属性，也不需要修改 HTML。

![预期结果](./desired-outcome.png)

### 自检
使用此部分检查你的工作。在这些项目中，你的目标不是达到 100% 的像素完美，而是使用你所学的工具来相对接近预期的输出。

- Div One 和 Div Three 的文本和边框之间有 32px 的距离。
- Div One 与页面上任何其他元素之间有 12px 的距离。
- Div Two 和 Div Three 之间有 48px 的间隙。
- Div Three 向右对齐。
- Div Three的对齐是使用 `margin` 实现的（而不是float、flexbox等）。